# This is docker-compose file will use the compose/build services/apps containers from instructions
version: "2"
services:
  app:
    image: node:10
    working_dir: /app
    environment:
      - NODE_ENV=production
      - PORT=8080
    # volumes:
    #   - .:/app
    ports:
      - 8080:8080
    # command: "cd /app && ls -la"
    # command: "echo $PWD"
    command: bash -c "ls -la"
    # command: "npm start"


# TODO: Not working